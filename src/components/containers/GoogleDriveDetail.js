import React, { Component } from 'react';
import { Text } from 'react-native';

export default class GoogleDriveDetail extends Component {
  render() {
    return (
      <Text>
        Google Drive Details
      </Text>
    );
  }
}