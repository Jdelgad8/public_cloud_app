import React, { Component } from 'react';
import { Text } from 'react-native';

export default class AzureDetail extends Component {
  render() {
    return (
      <Text>
        Azure Details
      </Text>
    );
  }
}